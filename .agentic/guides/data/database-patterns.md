# Database Patterns — playwright-ai-triage

> Generated by agentic-os guide-generator (0.3.0).
> owner: generated — regenerate via /agentic-upgrade; edits survive as owner: user.

## A. No managed schema — escalate schema work to a human

This repository has no database. Runtime dependencies are exactly
`@anthropic-ai/sdk` and `zod`; there is no migrations directory, no ORM, and no
persistence code anywhere in the two source files.
Evidence: `package.json` — `dependencies` block lists only `@anthropic-ai/sdk` and `zod`.
Evidence: `src/index.ts`, `src/types.ts` — the entire source tree; a reporter class and its types, no storage.

**Rule: any work that appears to need persistence (caching classifications,
failure ledgers, run history) stops and goes to a human** under
`## Escalate to human`. Adding storage changes the product contract —
`README.md` states "There is no hosted platform behind this package" and the
setup is one config line plus an env var — so it is a design decision, not an
implementation detail. Do not invent schema rules for a stack that does not
exist here.
